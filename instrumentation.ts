/** Instrumentation hook — keep empty; use `npm run startup-check` for diagnostics. */
export async function register() {
  // Intentionally empty to avoid pulling Node-only modules into the Next.js build graph.
}
